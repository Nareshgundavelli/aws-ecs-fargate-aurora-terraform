# CloudMart Frontend Environment Variables
# Copy this file to .env.local and fill in your values for local development.
# In production (nginx), the API is proxied via relative /api paths.

# Base URL for the backend API.
# Leave empty to use relative /api paths (production / nginx).
VITE_API_URL=
